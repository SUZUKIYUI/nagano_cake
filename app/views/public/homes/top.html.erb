<div class="row">
  <div class="col-md-8 back-cake mx-auto">
    <p class="white-color text-center">ようこそ、ながのCAKEへ！<br>
      このサイトはケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。
    </p>
  </div>
</div>
<h1 class="mt-5 mb-3">新着商品</h1>
<div class="d-flex justify-content-between row">
<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item.id) do %>
      <%= attachment_image_tag item, :image, :fill, 200, 140 %><br>
      <%= item.name %><br>
      ¥<%= ((item.price + item.price*0.1).floor).to_s(:delimited) %>
    <% end %>
  </div>
<% end %>
</div>
<div class="text-right">
  <%= link_to "全ての商品を見る >", items_path, class: "btn-blue" %>
</div>
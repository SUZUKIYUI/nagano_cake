<div class="row wid-80 mx-auto mb-5">
  <h2 class="px-4 py-0 text-center my-3 back-color">注文履歴一覧</h2>
</div>

<table class="border-table mb-5">
  <thead>
    <tr>
      <th class="border-table">注文日</th>
      <th class="border-table">配送先</th>
      <th class="border-table">注文商品</th>
      <th class="border-table">支払金額</th>
      <th class="border-table">ステータス</th>
      <th class="border-table">注文詳細</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td class="border-table">
          <%= order.created_at.strftime('%Y/%m/%d') %>
        </td>
        <td class="border-table">
          〒<%= order.delivery_postal_code %><br>
          <%= order.delivery_address %><br>
          <%= order.customer.last_name + order.customer.first_name %>
        </td>
        <td class="border-table">
          <% order.order_items.each do |order_item| %>
            <%= order_item.item.name %>
          <% end %>
        </td>
        <td class="border-table">
          <%= order.total_price %>円
        </td>
        <td class="border-table">
          <%= order.order_status_i18n %>
        </td>
        <td class="border-table text-center">
          <%= link_to "表示する", order_path(order.id), class: "btn btn-primary px-2 py-1" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>





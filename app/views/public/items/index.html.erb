<h2>商品一覧</h2><span>全<%= @items.count %>件</span>
<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= attachment_image_tag item, :image, :fill, 100, 80 %>
    <%= item.name %>
    ¥<%= ((item.price + item.price*0.1).floor).to_s(:delimited) %>
  <% end %>
<% end %>

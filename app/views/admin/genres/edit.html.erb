<h1>ジャンル編集</h1>

<%= form_with model:@genre,url:admin_genre(@genre.id), local: true do |f| %>
  <%= f.label :name, "ジャンル名" %>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存" %>
<% end %>

<!--bootstrapで整える-->
<!--未入力の項目があればはじき返す-->
<!--エラー文の表示-->
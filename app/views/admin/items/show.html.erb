<h1>商品詳細</h1>

<%= attachment_image_tag @item, :image, :fill, 60, 60, fallback: "no_image.jpg" %>

<p>商品名</p><%= @item.name %>
<p>商品説明</p><%= @item.introduction %>
<p>ジャンル</p><%= @item.genre.name %>
<p>税込価格<br>（税抜価格）</p><%= @item.price + @item.price*0.1 %><br><%= "(#{@item.price})" %>円
<p>販売ステータス</p><%= @item.is_active %>

<%= link_to "編集する", edit_admin_item(@item.id) %>

<!--bootstrapで整える-->
